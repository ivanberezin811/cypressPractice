version: 2.1
orbs:
  cypress: cypress-io/cypress@3
workflows:
  build:
    environment:
      PROJECT_ID: $PROJECT_ID
    jobs:
      - cypress/run:
          #install-browsers: true
          cypress-command: 'npm run e2e:run:cypress:dashboard:ci'