name: Cypress Tests

on: push

jobs:
  cypress-run:
    runs-on: windows-2022
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Cypress run
        uses: cypress-io/github-action@v6
        with:
          start: npm run e2e:run:cypress:dashboard